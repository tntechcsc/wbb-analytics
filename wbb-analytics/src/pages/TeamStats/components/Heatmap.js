import React from 'react';
import './Heatmap.css'; // Ensure you're importing the CSS file

function Heatmap({ shotData }) {
  // Render a div that will be styled as a red box
  return <div className="heatmap-placeholder"></div>;
}

export default Heatmap;